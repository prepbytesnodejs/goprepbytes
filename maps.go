package main

import "fmt"

/**

{
	"key":"value"
}
**/

func mapMethod() {

	//key value

	//HAsh map
	// Dictionary

	// In array
	mp := make(map[string]int)
	mp["name"] = 8
	mp["name"] = 9
	fmt.Println(mp)

	// the complexity of getting the value out of a map is O(1);
	// the complexity of inserting /putting anything is O(1)

}

// [2,3,5,7,8];
// you have to right a function that find  the index of the numbers that adds to a particular number
// func getIndexes(9){  }
